---
collections:
  - name: kubernetes.core
    version: ">=6.0.0,<7.0.0"
  - name: community.general
    version: ">=12.0.0,<13.0.0"
  # redhat.openshift (listed in AGENTS.md) is not used by the current roles and is not on
  # public Galaxy (Automation Hub only); add it with an Automation Hub server when needed.
