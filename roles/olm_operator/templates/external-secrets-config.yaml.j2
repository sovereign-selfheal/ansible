---
# Singleton CR of the Red Hat External Secrets Operator: the operator deploys the
# external-secrets controller, webhook and cert-controller only after it exists.
apiVersion: operator.openshift.io/v1alpha1
kind: ExternalSecretsConfig
metadata:
  name: {{ name }}
  labels:
    app: external-secrets-operator
spec: {}
